#pragma once
#include <string>
#include <vector>
#include <iostream>

namespace TopSystem
{
  using std::vector;
  using std::string;

  const unsigned int SCR_WIDTH = 800;
  const unsigned int SCR_HEIGHT = 600;
}